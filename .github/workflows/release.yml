name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to release, for example v2026.9.8.0 or v2026.9.8.0-beta'
        required: true

permissions:
  contents: write

concurrency:
  group: changelog-append
  cancel-in-progress: false

jobs:
  release:
    name: build and publish
    runs-on: windows-2025-vs2026
    timeout-minutes: 40

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Resolve release tag
        id: release_tag
        uses: RealWhyKnot/workflows/resolve-tag@v1
        with:
          tag: ${{ inputs.tag }}
          tag-pattern: '^v\d{4}\.\d+\.\d+\.\d+(-[A-Za-z0-9][A-Za-z0-9.-]*)?$'
          prerelease-pattern: '^v\d{4}\.\d+\.\d+\.\d+-.+$'

      - name: Set up .NET 10 SDK
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: '10.0.x'

      - name: Cache NuGet packages
        uses: actions/cache@v6
        with:
          path: ~/.nuget/packages
          key: nuget-release-${{ runner.os }}-${{ hashFiles('**/*.csproj', 'Directory.Build.props') }}
          restore-keys: |
            nuget-release-${{ runner.os }}-

      - name: Validate workflow scripts
        shell: pwsh
        run: |
          ./.github/scripts/Test-WorkflowSyntax.ps1
          ./.github/scripts/Test-UpdateChangelog.ps1
          ./.github/scripts/Test-GenerateReleaseNotes.ps1
          ./.github/scripts/Test-NightlyBetaPlan.ps1

      - name: Validate release version sequence
        uses: RealWhyKnot/workflows/version-sequence@v1
        with:
          tag: '${{ steps.release_tag.outputs.tag }}'
          prerelease-suffix: '[A-Za-z0-9][A-Za-z0-9.-]*'

      - name: Promote Unreleased to tagged section
        shell: pwsh
        run: |
          ./.github/scripts/Update-Changelog.ps1 -Mode Promote -Version '${{ steps.release_tag.outputs.tag }}'
          New-Item -ItemType Directory -Force -Path .changelog-stash | Out-Null
          Copy-Item CHANGELOG.md .changelog-stash/CHANGELOG.md -Force

      - name: Build release assets
        shell: pwsh
        run: |
          $version = '${{ steps.release_tag.outputs.tag }}' -replace '^v', ''
          ./build.ps1 -Version $version -Release

      - name: Write integrity manifest
        id: assets
        shell: pwsh
        run: |
          $tag = '${{ steps.release_tag.outputs.tag }}'
          $zip = Get-ChildItem release -File -Filter '*.zip' | Select-Object -First 1
          if (-not $zip) { throw "Expected a zip release asset under release/." }

          $integrityName = "MultiWindowShare-$tag.integrity.tsv"
          $integrityPath = Join-Path "release" $integrityName
          "name`tsha256`tbytes" | Set-Content -LiteralPath $integrityPath -Encoding utf8
          $hash = (Get-FileHash -Algorithm SHA256 -LiteralPath $zip.FullName).Hash.ToLowerInvariant()
          "$($zip.Name)`t$hash`t$($zip.Length)" | Add-Content -LiteralPath $integrityPath -Encoding utf8

          "zip_name=$($zip.Name)" >> $env:GITHUB_OUTPUT
          "integrity_name=$integrityName" >> $env:GITHUB_OUTPUT
          "integrity_path=$integrityPath" >> $env:GITHUB_OUTPUT

      - name: Write release notes
        shell: pwsh
        run: |
          ./.github/scripts/Generate-ReleaseNotes.ps1 `
            -Tag '${{ steps.release_tag.outputs.tag }}' `
            -Repo '${{ github.repository }}' `
            -ZipName '${{ steps.assets.outputs.zip_name }}' `
            -IntegrityName '${{ steps.assets.outputs.integrity_name }}' |
            Set-Content -Path release/notes.md -Encoding utf8
          if (-not (Test-Path release/notes.md) -or (Get-Item release/notes.md).Length -eq 0) {
            throw "Release notes were not generated."
          }

      - name: Publish GitHub release
        uses: RealWhyKnot/workflows/publish-release@v1
        with:
          tag: ${{ steps.release_tag.outputs.tag }}
          notes-file: release/notes.md
          prerelease: ${{ steps.release_tag.outputs.prerelease }}
          delete-existing: 'true'
          assets: |
            release/${{ steps.assets.outputs.zip_name }}
            ${{ steps.assets.outputs.integrity_path }}

      - name: Restore the promoted changelog
        if: ${{ steps.release_tag.outputs.prerelease != 'true' }}
        shell: pwsh
        run: Copy-Item .changelog-stash/CHANGELOG.md CHANGELOG.md -Force

      - name: Push the promoted changelog back to main
        if: ${{ steps.release_tag.outputs.prerelease != 'true' }}
        continue-on-error: true
        uses: RealWhyKnot/workflows/commit-on-branch@v1
        with:
          headline: 'docs(changelog): promote Unreleased to ${{ steps.release_tag.outputs.tag }} [skip changelog]'
          body: 'Promoted after publishing ${{ steps.release_tag.outputs.tag }}.'
          paths: |
            CHANGELOG.md
